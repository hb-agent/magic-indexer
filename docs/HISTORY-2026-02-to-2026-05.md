# Magic-Indexer history — Feb 2026 → May 2026

A chronological record of every meaningful change to land on `main` over the project's first three months. Generated from git history (`git log --since="2026-02-13" --until="2026-05-14" origin/main`); commit subjects link back to PR numbers where applicable.

**Headline arc:** project started 5 Feb 2026 as a Go port of Quickslice / `hyperindex`, was forked and rebranded to Magic Indexer on 11 Apr 2026, absorbed a comprehensive security audit + Postgres-only migration on 13 Apr, gained the notifications subsystem on 14 Apr, then continued shipping per-feature work through mid-May. Latest landing on 13 May 2026 was a 12-track review-follow-up bundle.

---

## At a glance

| Period | Commits | Theme |
|---|---|---|
| 5–6 Feb 2026 | 14 | Bootstrap (Phases 1-7) — schema, OAuth, admin API, frontend |
| 10–11 Feb 2026 | 6 | Small fixes from upstream contributors (consumer start fix, env doc, healthcheck timeout) |
| 11 Apr 2026 | 33 | Fork adoption, rebrand to Magic Indexer, labeler ingestion + 4-round review |
| 12 Apr 2026 | 13 | Trusted-evaluator authors filter, Postgres compatibility prep |
| 13 Apr 2026 | 63 | **Big push** — security audit (CS-001 → CS-015), Postgres-only migration, hyperindex port phases 0–7, sorting / totalCount / backward pagination, OR composition + nested filters, expression-index admin mutations, broken-record monitoring, design tokens & dark mode, full-text search |
| 14 Apr 2026 | 6 | Notifications subsystem (Bluesky-pattern aggregated) + ATProto service-auth JWT |
| 9 May 2026 | 1 | Server-side `excludePds` filter + `pds` field on every record |
| 13 May 2026 | 9 | Today — contributor filter, BadgeAward subject filter, layered query budgets, admin actor-purge subsystem, six-reviewer audit + 12-track follow-up |

Total: 141 commits across 7 distinct working days on `main` after 13 Feb 2026.

---

## February 2026 — bootstrap

Project foundation. Originally named **Hypergoat** then renamed in-repo to **Hyperindex (hi)**; both names appear in commit messages from this period. The binary is still called `hypergoat` today as a historical artifact.

### 5–6 Feb — initial implementation (Phases 1–7)

A single very productive day shipping the indexer's bones:

- `49c8c6b` **feat:** project setup, foundation layer, and lexicon parsing (Phases 1-2)
- `da8ec30` **feat:** GraphQL schema, resolvers, Jetstream, backfill, and subscriptions (Phases 3-4)
- `e16428b` **feat:** OAuth 2.0 with PKCE, DPoP, and auth middleware (Phase 5)
- `24c770e` **feat:** admin GraphQL API with background workers (Phase 6)
- `b9128b9` **feat:** Next.js admin frontend with GraphiQL playgrounds (Phase 7)
- `a2a6a66` **perf:** backfill optimizations, integration tests, and deployment setup
- `f82b4e3` **feat:** activity tracking, UI redesign, and GainForest branding
- `c330d6a` **fix:** deployment config, dynamic Jetstream subscriptions, and OAuth improvements
- `d851f43` **feat:** API documentation pages, pagination fixes, and UI polish
- `5d9b5ca` **feat:** collection analytics and startup schema loading
- `9269938` **rename:** Hypergoat → Hyperindex (hi) with project history
- `0d95edf` **fix:** address 8 security vulnerabilities from codebase review
- `57524f7` **refactor:** Phase 2 architecture cleanup (9 of 10 tasks)
- `cd6f9ce` **fix:** resolve all golangci-lint v2 issues for CI
- `38f6bc7` **refactor:** decompose `run()` god function into focused setup phases
- `045d8d1` **test:** Phase 3 — comprehensive test coverage for 6 untested packages
- `ecbe312` + `f6b7566` **feat:** Phase 4 production readiness — pagination, event reliability, CORS

### 10–11 Feb — small upstream fixes

- `5716275` **fix:** combine consecutive string params (gocritic)
- `480ebf7` **chore:** simplify JWK generation
- `89e1439` **fix:** start jetstream consumer when lexicons registered individually
- `3900389` **docs:** discourage AI from dangerous pushes
- `5719bb2` **fix:** increase healthcheck timeout and document security env vars
- `b3b2c52` **fix:** WebSocket origin checker handles empty `ALLOWED_ORIGINS`
- `5f1207e` **docs:** add all missing env vars to `.env.example`

**Then ~2 months of quiet** while the project sat untouched in this state.

---

## April 2026 — fork, rebrand, and the big push

### 11 Apr — fork adoption + labeler ingestion + 4-round review

The day Holke / `hb-agent` took ownership of the fork. 33 commits.

#### Rebrand + ops baseline

- `99fc290` **feat:** Rebrand product-facing docs as Magic Indexer
- `bc83188` **docs:** README clone URL → `hb-agent/magic-indexer`
- `e809a1d` **docs:** deployment runbook + 23-round review history
- `308efc9` **docs(AGENTS.md):** self-sufficient onboarding for a fresh AI session
- `f4e4169` **feat:** `OptionalAuth` passes through on invalid OAuth token
- `e18f68c` + `311de4b` **feat(cors):** wildcard origin support + agent-browser tooling docs
- `8dd5655` **fix:** normalise empty `event_json` to JSON null in activity log

#### Labeler ingestion (new subsystem)

- `ee8d238` **feat:** labeler ingestion + label-filtered record queries
- `5329b62` **fix:** Round 1 — protocol, safety, observability
- `83151b6` **fix:** Round 2 — lifecycle, ctx ordering, collisions
- `025e522` **fix:** Round 3 — idempotency, collisions, SSRF, caps
- `d1eb2cc` **fix:** Round 4 — Postgres boolean, migration safety, parse
- `4380b2d` **feat:** neutral multi-labeler + deferred-issues sweep
- `7455767` **feat(labels):** per-labeler `label_definition` semantics

#### Upstream cherry-picks + admin auth

- `46ad860` **fix:** relative paths in GraphiQL (multi-domain support)
- `b5f48ca` **feat:** replace `TRUST_PROXY_HEADERS` with `ADMIN_API_KEY` for admin auth
- `f3c5a21` **feat:** adopt useful pieces from upstream PRs #31, #32, #34
- `4d14c8a` **test:** require `TEST_DATABASE_URL` (stop honouring `DATABASE_URL` in tests)

#### Comprehensive review (8 rounds)

- `b5d08ef` Round 1 — security, concurrency, GraphQL hygiene
- `80a9c4e` Round 2 — expiration, DoS caps, pool hygiene
- `68c526e` Round 3 — hardening, URL validation, admin clamps
- `322ad09` Round 4 — headers, repro build, config warnings
- `a7aae92` Round 5 — DPoP replay race, rollback atomicity
- `f9e8c11` Round 7 — regression tests
- `6d5e149` Round 8 — token rotation order + error leakage
- `d8edda8` Round 14 — jetstream consumer state races

#### Operability + deployment hardening

- `cb7aa80` **feat:** deployment hardening — `SECRET_KEY_BASE` guard, non-root Dockerfile, CI Postgres + repro
- `c5a1a5e` **feat:** GraphQL depth guard, GraphiQL CSP, lexicon depth, DPoP sentinels
- `22b0f48` **feat:** operator runbook + durability — pause, label-chain, orphan janitor
- `12cb71b` **feat:** observability, ops docs, migration round-trip, deploy ease

### 12 Apr — trusted-evaluator authors filter + Postgres prep

13 commits — narrower scope than the day before.

#### Authors filter
- `5426af7` **feat:** trusted-evaluator authors filter for record feed queries
- `5b8c793` **feat:** architecture docs + admin client OAuth fixes
- `763cbf6` + `c07b91c` Round 1 review + docs updates

#### Deep review fixes
- `0718b8a` **fix:** deep code review — 30 fixes across security, concurrency, performance
- `9d3d330` **fix:** Round 2 — singleflight cache, data race, minor hardening

#### Postgres compatibility prep
- `5d30e3c` + `2b6f68b` lint cleanups
- `8b2a4b0` + `a650d20` **fix:** proper Postgres BOOLEAN types for `neg` / `require_redirect_exact`
- `9e5c1e2` **fix:** Postgres boolean and JSONB compatibility across all repos
- `c4bd7c1` **fix:** Postgres test compatibility — table names, bool types, cursor precision
- `9d67cf3` **docs:** CHANGELOG with release notes for deep code review

### 13 Apr — the big push (63 commits)

Five-track day: security audit, full Postgres migration, hyperindex port phases, multiple GraphQL features, broken-record monitoring, design tokens + dark mode.

#### Record-validation foundation
- `907b315` **feat:** record validation against lexicon schemas (#27)
- `cc7231d` **fix:** wire backfill validation + proper grapheme cluster counting
- `309c3fa` **feat:** broken record monitoring — `is_valid` column, validation stats, dashboard

#### Design tokens + dark mode (client UI overhaul)
- `db2e768` CSS design tokens with dark mode, remove geometric bg
- `dcc2832` update dashboard page & components with design tokens
- `32275d4` monochrome Button with design tokens
- `e6d83b0` update Card, Input, Alert with design tokens
- `86d7b96` update layout — dark mode, wider content
- `8dfb12b` frosted glass header with theme toggle + design tokens
- `a634571` `ThemeToggle` component for dark/light mode switching

#### Postgres-only migration (remove SQLite)
- `308f6ab` **refactor:** remove SQLite executor, migrations, and imports
- `ab83e96` **refactor:** remove dialect branches from all repositories
- `b82ed71` **refactor:** simplify Executor interface — remove dialect-specific methods
- `756a84e` **refactor:** update infrastructure for Postgres-only
- `f5e2c7d` + `1b1903f` + `7e3c019` migration safety fixes

#### Full-text search
- `c8d0117` **feat:** add full-text search parameter to all collection queries (#30)

#### Collection overview
- `fcaf42c` **feat:** collection overview — per-lexicon record counts with invalid count

#### Security audit (CS-001 → CS-015)
A self-numbered series of 15 fixes:

| CS-# | Commit | Subject |
|---|---|---|
| CS-001 | `33207fc` | Pin Go to 1.23 and Alpine to 3.21 |
| CS-002 | `c2e0ee8` | Remove hardcoded cookie secret default |
| CS-003 | `6dcb0da` | Remove hardcoded production Railway URLs from docs |
| CS-004 | `dcda0b8` | Stop exposing OAuth client secrets to browser |
| CS-005 | `e841839` | Add `gosec` and `bodyclose` linters |
| CS-006 | `5cc30a3` | Add `permissions: read-all` to CI workflow |
| CS-007 | `37fcf5d` | Harden session cookie + document `ADMIN_API_KEY` |
| CS-008 | `a30cec5` | Require authentication on admin GraphQL proxy |
| CS-009 | `88be4e6` | Replace hand-rolled JWT signing with `golang-jwt` |
| CS-010 | `549d47e` | Stop swallowing `createClientAssertion` errors |
| CS-011 | `eafe31d` | Security response headers on Next.js client |
| CS-012 | `c19dca7` | `.env.example` cleanup — remove example admin DID |
| CS-013 | `bd5deea` | Pin `opencode` plugin to specific version |
| CS-014 | `3912afd` | Request body size limit on public GraphQL proxy |
| CS-015 | `08cbaeb` + `ae0be91` | Upgrade `golang.org/x/crypto` to v0.31.0 + restore `GOTOOLCHAIN=auto` |

Plus `45e5dfe` (comprehensive audit report + CHANGELOG) and several `fix(lint)` follow-ups for the new linters.

#### Hyperindex port (phases 0–7)
A port of additional features from a sibling project:

- `cd29b05` Phase 0+1 — shared infra extraction + GraphQL field filters
- `c60e7ed` Phase 2+3+4 — sorting, `totalCount`, backward pagination
- `acc4230` Phase 5 — Tap consumer for crypto-verified event ingestion
- `74c61fd` Phase 6 — Tap/Jetstream toggle + collection auto-discovery
- `e15195c` Phase 7 — documentation updates
- `05953d0` Address review findings — panics, dead code, race, test quality
- `4d979f7` Resolve 10 golangci-lint issues

#### Additional features (issues #37–#48)
- `23f3b12` **test:** improve `createClientAssertion` test coverage (#37)
- `f78ce96` **feat:** `_and` / `_or` boolean composition on field filters (#38)
- `0be64f3` **feat:** sort-aware keyset pagination for custom `orderBy` (#39)
- `58d6444` **feat:** nested JSON field filtering via `__` path separator (#40)
- `1522424` **feat:** admin mutations for expression index management (#43)
- `dc0653c` **fix:** skip unfiltered fast path when custom sort is requested (#50)

### 14 Apr — notifications + service-auth

Six focused commits closing out a major notifications subsystem.

- `ce21994` **feat:** notifications subsystem (Bluesky pattern aggregated) (#54)
- `c97c222` **docs:** notifications subsystem documentation (#55)
- `c1b8601` **feat:** bundled PR for issues #22 / #24 / #26 / #33 / #53 (#56)
- `84f7fb4` **feat:** ATProto service-auth for notifications (#57) (#58)
- `2c11982` **fix:** collection-overview SQL rejected by strict Postgres (#59)
- `561ef70` **docs:** service-auth runbook + security surface (#60)
- `79356b6` **fix:** notifications aggregated upsert SQLSTATE 42P10 (#61) (#62)
- `5cfbc17` **docs:** hyperindex port + follow-up features (#37-#43, #50) (#52)

**Then ~3 weeks of quiet** between mid-April and early May.

---

## May 2026

### 9 May — server-side `excludePds` filter

A single feature PR:

- `a4ebe35` **feat:** server-side `excludePds` filter + `pds` field on every record (#63)

Per-actor PDS denormalisation; resolves PDS at ingestion with the existing `DIDCache` singleflight; PDS-backfill CLI; rate-limited PLC resolution; `idx_actor_pds` partial index migration.

### 13 May — feature day + six-reviewer audit

Nine landings in chronological order. The first four are individual feature PRs; the final two are review-driven multi-track follow-ups.

#### Feature PRs
- `fc1ccc0` **feat:** contributor filter on activity records + notifications fix + `did.IsValid` migration (#64) (#69) — strict DID validation rolled out across input boundaries; new `where: { contributor: ... }` shape on `orgHypercertsClaimActivity`.
- `bfb6683` **chore(testdata):** sync `app/certified` lexicons with canonical (#67)
- `d8c52c0` **feat(graphql):** `BadgeAward` subject filter (issue #65) (#75)
- `0d6cc97` **chore(testdata):** sync 4 `org/hypercerts` lexicons (#73)
- `c59b488` **feat:** layered query budgets on `/graphql` (#71) (#76) — pool-default `statement_timeout` + per-request middleware deadline; `QUERY_TIMEOUT` GraphQL error shape pinned.
- `a9d54df` **Promote staging to main:** admin actor-purge + client + config fixes + docs (#77) — `previewPurgeActor` / `purgeActor` HMAC-bound flow; `EXTERNAL_BASE_URL` normalisation; admin-DID gate on settings UI; batch lexicon registration; OAuth callback `PUBLIC_URL`; fail-fast on self-referential backend URL.
- `53d1c55` **fix(graphql):** `BadgeAward` subject filter matches `defs#did` object shape (#78)

#### Six-reviewer audit + 12-track follow-up
- `c4b579c` **review(2026-05-13):** P0 + selected P1 fixes (#79) — first 7 tracks of the follow-up landed
- `3521386` **review(2026-05-13) part 2:** 5 remaining tracks (#80) — wrapping the audit: ResetAll HMAC hardening, FilterKind enum refactor, partial-GIN expression index + STORED `subject_did` generated column, purge metrics + admin audit logs + `logsafe` package, resolver + Postgres-backed shape tests

Provenance for #79/#80 lives in `docs/review-2026-05-13/` — six independent reviewer passes (security, code-quality, performance in round 1; verification, test coverage, architecture in round 2) condensed into a prioritized action plan.

---

## Thematic overlay

Reading the timeline by theme instead of chronologically:

### Security

| When | What |
|---|---|
| 5 Feb | Initial OAuth 2.0 + PKCE + DPoP + auth middleware (Phase 5) |
| 11 Apr | DPoP sentinels, GraphiQL CSP, depth guards |
| 13 Apr | CS-001 → CS-015 audit (15 fixes — body size, JWT signing, secret hygiene, admin auth, CSP, response headers) |
| 13 May | Strict `did.IsValid` rollout, `ADMIN_API_KEY` consolidation, OAuth `returnTo` allow-list, admin auth-before-decode, purge HMAC, ResetAll HMAC, `internal/logsafe`, lint guard against `strings.HasPrefix(s, "did:")` |

### Schema / GraphQL surface

| When | What |
|---|---|
| 5 Feb | Initial GraphQL schema + resolvers + subscriptions |
| 11 Apr | Labeler ingestion → label-filtered record queries |
| 12 Apr | Trusted-evaluator authors filter |
| 13 Apr | Sort-aware keyset pagination, `totalCount`, backward pagination, `_and`/`_or` composition, nested filters via `__`, expression-index admin mutations, full-text search |
| 14 Apr | Notifications subsystem (aggregated Bluesky pattern) + service-auth JWT |
| 9 May | `excludePds` filter + per-record `pds` field |
| 13 May | Contributor filter on activity, BadgeAward subject filter, query budgets, actor purge mutations, `FilterKind` enum refactor, partial-GIN expression index + STORED `subject_did` column |

### Database / migrations

| When | What |
|---|---|
| 5 Feb | Initial schema |
| 11 Apr | `label_definition` per-labeler scope; migration round-trip in CI |
| 13 Apr | Remove SQLite — Postgres-only; broken-record monitoring; full-text search; collection overview |
| 13 May | `actor.pds` column + index (migration 019/020); migration 013 collision fix (021/022); contributor identities GIN expression index (023/024); STORED `subject_did` generated column + partial btree (025/026) |

### Observability + ops

| When | What |
|---|---|
| 11 Apr | Operator runbook, pause / label-chain endpoints, orphan janitor, deployment hardening |
| 13 Apr | Validation stats dashboard, audit report, CI Postgres + reproducible build |
| 14 Apr | Service-auth runbook, security surface docs |
| 13 May | Layered query budgets with `X-Query-Timeout` header + canonical error code; pgx `CancelRequestContextWatcherHandler` (pool stability under timeout storms); five new Prometheus series (purge metrics, admin settings changes, reset_all); structured `event=*` audit logs across the admin surface; `internal/logsafe` slog scrubber |

### Client (Next.js admin)

| When | What |
|---|---|
| 5 Feb | Initial Next.js admin frontend (Phase 7) |
| 13 Apr | Design tokens, dark mode, `ThemeToggle`, frosted glass header, monochrome Button, Card/Input/Alert with tokens |
| 13 May | Settings page admin-DID gate, purge panel with retyped-DID confirm + countdown, batch lexicon registration UI, OAuth callback uses `PUBLIC_URL`, settings form hydration fix (`useState` → `useEffect`), self-referential backend URL detection |

### Documentation

Substantial doc churn — operator-facing and agent-facing both:

- `docs/RUNBOOK.md` — operator playbook (April)
- `docs/AGENTS.md` — agent onboarding + project-specific deep-flow process (April + May)
- `SECURITY.md` — required env, rate limiting, HTTPS enforcement, service-auth surface, admin surface, metrics + audit-log contracts (multiple commits across April + May)
- `docs/review-2026-05-13/` — six-reviewer audit + plan + reviews + baseline (May 13)
- `docs/upstream-adoption/` — review docs for the late-April upstream-adoption work (May 13)
- `docs/issue-64/`, `docs/issue-65/`, `docs/issue-71/` — per-issue plans + review docs (May)

---

## Mechanical notes

- **Volume**: 141 commits since 13 Feb 2026; 170 commits all-time on `main`.
- **Authors**: every commit on `main` since 11 Apr is authored by `hb-agent`. Earlier commits (5 Feb – 11 Feb) carry various contributor emails from the pre-fork upstream.
- **Process maturity**: review rounds + numbered audits (`Round N`, `CS-NNN`, today's `T-XXX` track IDs) reflect a deliberate audit-and-fix cadence rather than ad-hoc work. The two biggest single-day pushes (13 Apr and 13 May) both ended with multi-reviewer audits and follow-up commits.
- **Quiet periods**: ~2 months Feb 11 → Apr 11 (pre-fork hibernation), ~3 weeks Apr 14 → May 9 (between notifications and `excludePds`), ~4 days May 9 → May 13 (between PR #63 and today's batch).

Generated 2026-05-13 from `git log --since="2026-02-13" --until="2026-05-14" origin/main`.
